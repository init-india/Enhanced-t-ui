# Gradle settings

# JVM memory for Gradle daemon
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Use AndroidX libraries instead of support libraries
android.useAndroidX=true

# Enable Jetifier to automatically migrate old support libraries
android.enableJetifier=true

# (Do NOT include android.enableAapt2, it’s obsolete)
